version: "3.9"

networks:
  loop-net:
    driver: bridge

volumes:
  loop-data:

services:
  telegram-bot:
    build: ./telegram-bot
    restart: unless-stopped
    ports:
      - "8100:8100"
    env_file:
      - loop.env
    environment:
      - PUBLIC_DOMAIN=${PUBLIC_DOMAIN:-your-domain.com}
    volumes:
      - loop-data:/app/data
    networks:
      - loop-net
    mem_limit: 256m
    cpus: "1.0"

  voice-bot:
    build: ./voice-bot
    restart: unless-stopped
    ports:
      - "8200:8200"
    env_file:
      - loop.env
    volumes:
      - loop-data:/app/data
    networks:
      - loop-net
    mem_limit: 256m
    cpus: "1.0"
